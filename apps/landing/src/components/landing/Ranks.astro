---
---

<section id="ranks" class="section ranks-section">
      <div class="section-label">Progression System</div>
      <h2 class="section-title">
        Climb the <span class="grad-text">Ladder</span>
      </h2>
      <p class="section-sub">
        Five competitive tiers. Each rank unlocks new cosmetics and achievement
        titles. Spirit Bear is reserved for the relentless.
      </p>

      <div class="ranks-grid" style="grid-template-columns: repeat(5, 1fr);">
        <div class="rank-card" style="--rank-clr:#C89B7B">
          <span class="rank-tier-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/></svg>
          </span>
          <span class="rank-name">Cub</span>
          <span class="rank-desc">Just getting started</span>
        </div>
        <div class="rank-card" style="--rank-clr:#4ADE80">
          <span class="rank-tier-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
          </span>
          <span class="rank-name">Panda</span>
          <span class="rank-desc">Building good habits</span>
        </div>
        <div class="rank-card" style="--rank-clr:#CD7F32">
          <span class="rank-tier-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
          </span>
          <span class="rank-name">Grizzly</span>
          <span class="rank-desc">Serious scholar</span>
        </div>
        <div class="rank-card" style="--rank-clr:#60A5FA">
          <span class="rank-tier-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3a6 6 0 0 0 9 9 9 9 0 1 1-9-9z"/></svg>
          </span>
          <span class="rank-name">Polar</span>
          <span class="rank-desc">Cool under pressure</span>
        </div>
        <div class="rank-card rank-mythic" style="--rank-clr:#F0A030">
          <span class="rank-tier-icon">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 4l3 12h14l3-12-6 7-4-7-4 7-6-7z"/></svg>
          </span>
          <span class="rank-name">Spirit Bear</span>
          <span class="rank-desc">Legendary status</span>
        </div>
      </div>

      <div class="achievements-row">
        <div class="section-label" style="margin-bottom:1.25rem;">
          Achievement Titles
        </div>
        <div class="achievement-tags">
          <span class="ach-tag">First Blood</span>
          <span class="ach-tag">Flawless Victory</span>
          <span class="ach-tag">7-Day Inferno</span>
          <span class="ach-tag">Perfect Recall</span>
          <span class="ach-tag">Speed Runner</span>
          <span class="ach-tag">Night Owl</span>
          <span class="ach-tag">No-Miss Month</span>
          <span class="ach-tag ach-locked">??? Spirit Secret</span>
        </div>
      </div>
    </section>

<style>
/* ── RANKS ── */
  .ranks-section {
    border-top: 1px solid var(--border);
  }

  .ranks-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 0.75rem;
    margin-bottom: 3rem;
  }

  .rank-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 0.75rem;
    padding: 1.25rem 1rem;
    text-align: center;
    cursor: default;
    transition:
      border-color 0.3s,
      transform 0.3s,
      opacity 0.4s;
    opacity: 0;
    transform: translateY(16px);
  }

  .rank-card.revealed {
    opacity: 1;
    transform: translateY(0);
  }

  .rank-card:hover {
    border-color: var(--rank-clr);
    transform: translateY(-3px);
  }

  .rank-tier-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 0.5rem;
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: rgba(from var(--rank-clr) r g b / 0.1);
    color: var(--rank-clr);
  }

  .rank-name {
    display: block;
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.9rem;
    color: var(--rank-clr);
    margin-bottom: 0.25rem;
  }
  .rank-desc {
    display: block;
    font-size: 0.72rem;
    color: var(--muted);
  }

  .rank-mythic {
    background: linear-gradient(
      135deg,
      rgba(168, 85, 247, 0.08),
      rgba(123, 92, 245, 0.08)
    );
    border-color: rgba(168, 85, 247, 0.3);
  }

  .achievements-row {
    border-top: 1px solid var(--border);
    padding-top: 2rem;
  }

  .achievement-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .ach-tag {
    padding: 0.4rem 0.9rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    font-size: 0.8rem;
    color: var(--muted);
    background: var(--surface);
    transition:
      border-color 0.2s,
      color 0.2s;
  }

  .ach-tag:hover {
    border-color: rgba(123, 92, 245, 0.4);
    color: var(--text);
  }

  .ach-locked {
    opacity: 0.4;
    border-style: dashed;
  }

  @media (max-width: 480px) {
    .ranks-grid { grid-template-columns: repeat(2, 1fr); gap: 0.5rem; }
    .rank-card { padding: 1rem 0.75rem; }
    .rank-tier-icon { width: 30px; height: 30px; }
    .rank-name { font-size: 0.82rem; }
    .rank-desc { font-size: 0.68rem; }
    .ach-tag { padding: 0.35rem 0.7rem; font-size: 0.75rem; }
    .achievements-row { padding-top: 1.5rem; }
  }

  @media (max-width: 360px) {
    .ranks-grid { grid-template-columns: 1fr; }
  }
</style>
